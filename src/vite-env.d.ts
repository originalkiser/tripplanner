/// <reference types="vite/client" />

// Injected by vite.config.ts's `define` — a fresh id per production build,
// used by src/lib/version.ts to detect when a newer build has deployed.
declare const __BUILD_ID__: string
